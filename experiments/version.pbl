##This just displays the current version number.
 
define Start(par) {

  win <- MakeWindow()
  font <- MakeFont(gPEBLBasefont,0,40, MakeColor("black"), MakeColor("white"),0)
  lab <- MakeLabel(GetPEBLVersion(),font)
  Print(GetPEBLVersion())
  AddObject(lab,win)
  Move(lab, 300,200)
  Draw()
  WaitForAnyKeyPress()

  
}

